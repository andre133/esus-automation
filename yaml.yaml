services:
  - type: web
    name: esus-automation
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install
      playwright install-deps
    startCommand: gunicorn app:app